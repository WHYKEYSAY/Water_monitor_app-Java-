<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.groupel.elecoen390_watermonitor">

    <user-permission android:name="android.permission.CAMERA"></user-permission>

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE"></uses-permission>
    <application
        android:name=".app"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".QR_code"></activity>
        <activity android:name=".Meter">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".detailedInfo"
            android:parentActivityName=".Meter" />
        <activity android:name=".MainMenu" />
        <activity
            android:name=".TableActivity"
            android:parentActivityName=".Meter"></activity>
        <activity
            android:name=".graphActivity"
            android:parentActivityName=".TableActivity"></activity>

        <receiver
            android:name=".AlarmReceiver"
            android:process=":remote"></receiver>

        <service
            android:name=".AlarmService"
            android:exported="false"></service>

        <meta-data android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="barcode"></meta-data>
    </application>

</manifest>